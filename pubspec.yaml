name: migrate_notion_page_in_pubsub_message
description: A Dart microservice that retrieves a Notion page using the URL received in a Pub/Sub message.
version: 0.0.0-a1
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.0
  http: ^1.0.0
  json_utils: ^0.0.0-a1
  discord_application_utils: ^0.0.0-a1

dev_dependencies:
  enspyr_lints: ^0.0.0-a1
  test: ^1.21.0
